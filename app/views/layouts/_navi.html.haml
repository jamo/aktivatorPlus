.navi
  %ul.naviNoDots
    %li= link_to "Admin", admin_path
    %br/
    %li= link_to "New Course", new_course_path
    %br/
    %li= link_to "Show Courses", courses_path
    %br/
    - if params[:controller]=="courses" and params[:id] or params[:course_id]
      - @courseID = params[:course_id]
      - if params[:controller]=="courses"
        - @courseID = params[:id]
      - else
        - @courseID = params[:course_id] unless params[:course_id]== nil
      %li= link_to "Edit This Course", edit_course_path([@courseID])
      %br/
      %li= link_to "New Question", new_course_question_path([@courseID])
      %br/
      %li= link_to "Show Courses Questions", course_questions_path([@courseID])
      %br/
      %li= link_to "Show Courses Comments", course_comments_path([@courseID])
      %br/
    - if params[:course_id] and params[:id]
      %li= link_to "Answer to this quesion", new_course_question_answer_path(params[:course_id], params[:id])
      %br/
      %li= link_to "Show Questions Results", course_question_path(params[:course_id], params[:id])
      %br/
      %li= link_to "Show Questions Comments", course_question_comments_path(params[:course_id], params[:id])
      %br/
    %li= link_to "New User", new_user_path
    %br/
    %li
      = link_to "Show Users", users_path
    %br/
    = button_to "Logout",logout_path, :method => :delete
