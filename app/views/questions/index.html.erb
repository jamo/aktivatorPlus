<h1>Listing questions for </h1>
<%= @course.name%>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @course.questions.each do |courseT| %>
  <tr>
    <td><%= link_to "courseT.name", course_question_path(@course) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Question', new_course_question_path %>
