<h1>Listing questions for </h1>
<%= @course.name%>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @course.questions.each do |question| %>
  <tr>
    <td><%= link_to "#{question.name}", course_question_path(@course.id, question) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Question', new_course_question_path %>
