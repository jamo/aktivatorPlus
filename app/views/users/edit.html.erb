<h1>Editing user</h1>

<% if params[:user_id] == @user.id or @user.administrator? %>
  <%= render 'form' %>
  <% else %>
    <h2 style=" color: red;">Ei oikeutta muuttaa toisen tietoja!</h2>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
